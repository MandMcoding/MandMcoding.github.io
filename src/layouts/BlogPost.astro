---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  secret?: boolean;
}

const { title, description, pubDate, updatedDate, secret } = Astro.props;
---
<BaseLayout title={title} description={description} secret={secret}>
  <article>
    <h1>{title}</h1>
    <time datetime={pubDate.toISOString()}>
      {pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
    </time>
    {updatedDate && (
      <p>
        Updated:
        <time datetime={updatedDate.toISOString()}>
          {updatedDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
        </time>
      </p>
    )}
    <slot />
  </article>
</BaseLayout>
